# Supabase project credentials for ConstructionOS AI.
# Create a project at https://supabase.com, then apply supabase/migrations/*.sql
# (via `supabase db push` or the SQL editor) before running the app.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
